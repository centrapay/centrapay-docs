---
import { Code as ExpressiveCode } from "astro-expressive-code/components";

const {code, lang, title, mark} = Astro.props;
---

<ExpressiveCode
	code={code}
	title={title}
	class="mb-8"
	mark={mark ?? undefined}
	frame="terminal"
	lang={lang}
/>
