---
const { image, caption, imageWidth } = Astro.props;
---

<div class={`flex flex-col gap-2 ${imageWidth ? `max-w${imageWidth}` : ''} text-wrap`}>
  <img class={`block ${imageWidth ? `w-${imageWidth}` : '' }`} src={image} />
  <p class="font-normal text-xs text-center">
    {caption}
  </p>
</div>
